version: '3.7'

services:
  adoteumamanha-springbackend:
    image: adote-um-amanha/springbackend:latest
    ports:
      - "5030:8090"
    networks:
      - backend
    environment:
      - DB_URL=jdbc:postgresql://18.118.226.194:5432/ages

networks:
  backend:
    external: true
    driver: bridge